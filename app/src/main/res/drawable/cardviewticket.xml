<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Background color -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/Violet" />
        </shape>
    </item>

    <!-- Left-side hole -->
    <item android:left="16dp" android:top="50dp" android:bottom="50dp">
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

    <!-- Right-side hole -->
    <item android:right="16dp" android:top="50dp" android:bottom="50dp">
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

</layer-list>
